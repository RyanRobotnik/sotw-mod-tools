# Compiled output
dist/

# Dependencies
node_modules/

# clasp local credentials (contains OAuth tokens — never commit)
.clasprc.json

# clasp project config (contains user-specific script IDs — use .clasp.json.example as a template)
.clasp.json
clasp.dev.json
clasp.prod.json

# Source maps
*.js.map
